#include <stdio.h>

int main ()
{
    int line = 0;
    while(line < 30000)
    {
        printf("Ğ´´úÂë:%d\n",line);
        line++;
    }
    if(line == 30000)
    {
        printf("ºÃoffer\n");
    }
    return 0;
}